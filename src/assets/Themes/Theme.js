import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  status: {
    danger: "#e53e3e",
  },
  palette: {
    primary: {
      main: "#000",
      darker: "#000",
    },
    neutral: {
      main: "#64748B",
      contrastText: "#fff",
    },
  },
});
